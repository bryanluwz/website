export enum PagesEnum {
  "work" = "/",
  "about" = "about",
  "funsies" = "funsies",
  "contact" = "contact", // not in use anymore lol
}

export enum ChatbotRoleEnum {
  "user" = "user",
  "system" = "system",
}
